<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1.0, user-scalable=0">
    <title>{{repo_name}}</title>
    <link rel="stylesheet" type="text/css" href="web/index.css">
    <style>a, .tint {color: {{tint_color}}} .active {color: {{tint_color}}}</style>
    <meta name="description" content="{{repo_desc}}">
    <meta property="og:title" content="{{repo_name}}">
    <meta property="og:description" content="{{repo_desc}}">
    <meta property="og:image" content="{{repo_url}}/CydiaIcon.png">
    <meta name="twitter:card" content="summary">
    <meta name="twitter:title" content="{{repo_name}}">
    <meta name="twitter:description" content="{{repo_desc}}">
    <meta name="twitter:image" content="{{repo_url}}/CydiaIcon.png">
    <meta name="theme-color" content="{{tint_color}}">
</head>
<body>
    <div class="body">
        <div id="header">
            <h1 class="left_float">{{repo_name}}</h1>
            <button class="right_float add_repo tint" onclick="location.href='{{repo_url}}/add'">+</button>
        </div>
        <div class="scroll_view">
            {% for featured_tweak in featured_tweaks %}
            <a href="depiction/web/{{featured_tweak.control.Package}}.html" style="background-image: url(assets/{{featured_tweak.control.Package}}/banner.png)" class="card">
                <p>{{featured_tweak.control.Name}}</p>
            </a>
            {% endfor %}
        </div>
        <h2 class="title">Packages</h2>
        <div class="packageList">
            {% for tweak in tweaks %}
            <a class="subtle_link" href="depiction/web/{{tweak.control.Package}}.html">
                <div class="package">
                    <img src="assets/{{tweak.control.Package}}/icon.png">
                    <div class="package_info">
                        <p class="package_name">{{tweak.control.Name}}</p>
                        <p class="package_caption">{{tweak.control.Author}}</p>
                    </div>
                </div>
            </a>
            {% endfor %}
        </div>
        <div class="caption center footer">Aurixa v{{aurixa_version}} - Updated {{run_date}}</div>
    </div>
</body>
    <script src="web/index.js"></script>
</html>
